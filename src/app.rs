use ratatui::{prelude::Backend, Terminal};

pub struct App {
    // input_handler
    // table_state
}

impl App {
    pub fn new() -> Self {
        Self {}
    }

    pub fn main_loop<B: Backend>(&mut self, terminal: &mut Terminal<B>) {
        loop {
            //
        }
    }
}